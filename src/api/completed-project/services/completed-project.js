'use strict';

/**
 * completed-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::completed-project.completed-project');
