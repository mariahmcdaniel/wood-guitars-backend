'use strict';

/**
 * headstock-veneer-back controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::headstock-veneer-back.headstock-veneer-back');
