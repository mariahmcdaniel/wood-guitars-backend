'use strict';

/**
 * headstock-veneer-back router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::headstock-veneer-back.headstock-veneer-back');
