'use strict';

/**
 * headstock-veneer-back service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::headstock-veneer-back.headstock-veneer-back');
